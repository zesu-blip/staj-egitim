2-dizi, birden fazla değeri sırayla tutan yapı. dizi elemanlarına sıra numaralarıyla erişilir ve numara sıfırdan başlar. push kullanarak sona yeni eleman eklenir. diziadi.length yazarak dizideki eleman sayısını verir.
3-dizide her eleman sıra numarasıyla(index) dururken nesnede her değerin bir adı(key) vardır. gorev.metin ile görevin metnine, gorev.bitti ile tamamlanıp tamamlanmadığı bilgisine ulaşılır. gorev.bitti = false yazdığımda konsolda false, gorev.bitti = true yazdığımda ise true çıktısını aldım. böylece nesnenin içindeki bir değeri değiştirebildiğimi gördüm.
4-gorevler[0].metin yazdığımda dizinin ilk elemanı olan "ekmek al" konsola çıktı. aynı şekilde gorevler[1].metin yazdığımda da ikinci eleman konsola çıktı. for döngüsüyle diziyi gezidğimde de dizinin bütün elemanlarını konsola yazdırdım. sonra da if ile false olanları yazdırdım.
5-querySelector ile HTML elementlerini seçebiliriz. seçtiğimiz elementleri değişkenlere atayıp console.log ile konsolda görebiliriz. textContent ile de bir elementin içindeki yazıyı okuyup değiştirebiliriz.
6-addEventListener ile olay gerçekleştiğinde çalışacak kodu belrleriz. click butona tıklandığında gerçekleşen olayı belirtir ve fonksiyonun içindeki console.log("tıklandı") her tıklamada çalışır. önceden yazdığım fonksiyonların bir adı varken burada fonksiyonun adı yok ve doğrudan addEventListener içine yazılır.
7- input.value ile inputun içine yazılan değeri okuyabiliriz. typeof input.value çıktısının string olduğunu gördüm. yani inputa sayı yazsam bile değer string olarak geliyor. bu yüzden input.value + 5 işleminin sonucu 55 oldu. Number(input.value) + 5 işleminde ise Number() kullanarak değeri sayıya çevirdiğimiz için sonuç 10 oldu.
8-createElement ile yeni bir HTML elementi oluşturulur. appendChild ise oluşturduğumuz elementi başka bir elementin içine eklememizi sağlar.
10-addEventListener ile li elementine tıklama olayıekleddim ve tıkladığında li.remove ile o elementin listeden silinmesini sağladım. ama sayfayı yenilediğimde sildiğim görevlerin tekrar geldiğini gördüm. bunun sebebi, li.remove ile sadece sayfadaki elementin silinmesi, görevin js tarafındaki diziden siilinmemesi.
11-splice dizide istediğimiz elemanları silmek için kullanılır. ilk parametre kaçıncı elemandan başlanacağını, ikinci parametre ise kaç eleman silineceğini belirtir.
--innerHTML bir html elementinin içeriğini html olarak değiştirmeye yarar. textContent ise yalnızca düz metin ekler, html kodu çalıştırmaz, sadece yazı olarak gösterilir. kullanıcıdan gelen verileri ekrana yazdırırken çoğu zaman textContent kullanılır.