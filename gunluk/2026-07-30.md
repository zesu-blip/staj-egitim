bugün html, css ve git konularını kapsayan bir tekrar çalışması yaptım.
html'de `index.html` üzerindeki `<header>`, `<main>`, `<section>`, `<footer>` gibi semantic etiketleri inceledim.
form yapısındaki `<form>`, `<input type="text">` ve `<button>` elemanlarını kontrol ettim.
css'te style.css dosyasında flexbox kurallarını denedim. `display: flex;`, `justify-content: center;`, `align-items: center;`.
`color`, `font-family`, `margin` ve `padding` özelliklerini düzenleyip sayfa düzenini kontrol ettim.
`git status` ile çalışma dizinindeki değişiklikleri kontrol ettim.
`git log --oneline -n 5` ile son commit geçmişini inceledim.