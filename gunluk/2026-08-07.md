bugün javascript dosyasındaki eksikleri ve hataları düzelttim. İlk olarak `script.js` dosyasındaki yorum satırlarını düzenleyip kodları çalışır hale getirdim ve çakışan tanımları düzenledim.  problemler kısmında önceden kullandığım `prompt` satırlarını sildim ve prompttan gelen değerlerin string olmasıyla ilgili örnek yaptım.fonksiyonları parametre alacak şekilde güncelledim. Kod genelinde kullandığım `==` işaretlerini `===`  ile değiştirdim, tipler zaten aynı olduğu için sonuçların değişmediğini gördüm. `boolean` tipi için `typeof` çıktısını ve `gorevSayisi` değişkeninin farklı değerlerde verdiği konsol çıktılarını ekledim. cevaplar.md dosyasındaki eksik kısımları tamamladım ve farklı değerlerle yaptığım denemelerin sonuçlarını ekledim.